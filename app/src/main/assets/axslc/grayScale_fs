#version 100
precision mediump float;
precision highp int;

uniform mediump sampler2D u_tex0;

varying highp vec2 v_texCoord;
varying highp vec4 v_color;

void main()
{
    highp vec4 c = texture2D(u_tex0, v_texCoord);
    c = v_color * c;
    highp vec3 _47 = vec3(((0.2125999927520751953125 * c.x) + (0.715200006961822509765625 * c.y)) + (0.072200000286102294921875 * c.z));
    gl_FragData[0].x = _47.x;
    gl_FragData[0].y = _47.y;
    gl_FragData[0].z = _47.z;
    gl_FragData[0].w = c.w;
}

