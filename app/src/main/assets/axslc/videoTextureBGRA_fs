#version 100
precision mediump float;
precision highp int;

uniform mediump sampler2D u_tex0;

varying highp vec4 v_color;
varying highp vec2 v_texCoord;

void main()
{
    gl_FragData[0] = v_color * texture2D(u_tex0, v_texCoord).zyxw;
}

